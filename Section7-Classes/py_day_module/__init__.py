#from make_messages import MessageUser

# As soon as I add a "__init__.py" file inside a folder, python automatically recognizes the whole Folder
# as a python module (just as it recognizes the files with .py as modules too).

# We can also create sub-folders inside folders, add "__init__.py" file in them, to turn them into Python modules as well.
